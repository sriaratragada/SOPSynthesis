{
  "name": "@sops/extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.297",
    "typescript": "~5.7.3",
    "vite": "^6.0.7"
  }
}
